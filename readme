release guide:
  following instructions here: https://goreleaser.com/quick-start/
  export GITHUB_TOKEN=
  export GITTAG=v0.2.2
  git tag -a $GITTAG -m "structs update"
  git push origin $GITTAG
  goreleaser release --rm-dist

usage:
