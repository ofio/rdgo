release guide:
  following instructions here: https://goreleaser.com/quick-start/
  export GITHUB_TOKEN=
  git tag -a v0.1.4 -m "structs update"
  git push origin v0.1.4
  goreleaser release --rm-dist

usage:
